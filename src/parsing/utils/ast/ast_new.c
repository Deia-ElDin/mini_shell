
	// if (token->type == TOKEN_WORD)
	// {
	// 	new_node->cmd = ft_strdup(token->value);
	// 	if (!new_node->cmd)
	// 		exit_failure(data);
	// }
	// else
	// 	new_node->cmd = NULL;
	// new_node->args = NULL;